# Alembic Migrations

To generate your initial database migration, run the following command from the `/backend` directory:

```bash
alembic revision --autogenerate -m "Initial migration"
alembic upgrade head
```
